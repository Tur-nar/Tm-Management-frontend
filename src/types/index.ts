export interface Feature {
    title: string;
    description: string;
}
export interface Testimonial {
    name: string;
    role: string;
    company: string;
    comment: string;
}

export interface HowItWorks {
    step: number;
    title: string;
    description: string;
}